<div class="container">
    <div class="row">
        {{#each movies}}

        <div class="col-md-3">
            <img src="{{image}}" alt="{{title}}">
            <h4>{{title}}</h4>
            <p>{{description}}</p>
            <a href="/pelicula/crear-localizacion/{{id}}" class="btn btn-dark">Elegir</a>
        </div>

        {{/each}}

    </div>
</div>