<h2>Listado</h2>




<div class="container">
    <div class="row">
        {{#each movie}}

        <div class="col-md-3">
            <img src="{{image}}" alt="{{title}}">
            <h4>{{title}}</h4>
            <p>{{description}}</p>
            <a href="/peliculas/detalles/{{_id}}" class="btn btn-dark">Ir a detalles</a>
        </div>

        {{/each}}

    </div>
</div>