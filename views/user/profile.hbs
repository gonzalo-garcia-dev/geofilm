<div class="container">
    <h5>Hola, {{findUser.username}}</h5>
    <hr>
    <h4>Tus películas</h4>

    <div class="container">
        <div class="row">
            {{#each movies}}

            <div class="col-md-4 ">
                <div class="card mb-4">
                    <img src="{{image}}" class="card-img-top" alt="{{title}}">
                    <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text">{{director}}</p>
                        <p class="card-text">{{year}}</p>
                        <a href="/peliculas/detalles/{{_id}}" class="btn btn-dark">Detalles</a>

                    </div>
                </div>
            </div>
            {{!-- <div class="card" style="width: 30rem;">
                <div class="col-md-3 ">
                    <img src="{{image}}" class="card-img-top" alt="{{image}}">
                    <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <ul>
                            <li>
                                <p class="card-text">{{year}}</p>
                            </li>
                            <li>
                                <p class="card-text">{{director}}</p>
                            </li>
                            <li>
                                <p class="card-text">{{stars}}</p>
                            </li>
                        </ul>

                    </div>
                </div>

            </div> --}}
            {{/each}}
        </div>

    </div>
</div>